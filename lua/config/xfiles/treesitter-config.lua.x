require'nvim-treesitter.configs'.setup {
  ensure_installed = "maintained",
  context_commentstring = {
    enable = false
  },
  highlight = {
    enable = false,
  },
  indent = {
    enable = false
  }
}

